<h1>タスク新規作成</h1>

<% if @task.errors.any? %>
<% @task.errors.full_messages.each do |error| %>
<li><%= error %></li>
<% end %>
<% end %>

<%= flash.now[:alert] %>
<%= form_with(model: @task) do |f| %>
<%= f.label :name, "タスク名" %>
<%= f.text_field :name %>

<%= f.label :content, "内容" %>
<%= f.text_area :content, size: "30*5" %>

<%= f.label :expiration_deadline, "終了期限" %>
<%= f.datetime_local_field :expiration_deadline %>

<%= f.submit "登録" %>
<% end %>
